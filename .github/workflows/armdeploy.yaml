name: ARM Template Deploy Workflow

on:
  push:
    branches: ['master']
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy ARM Template
        run: |
          az group create --name DeployARM --location useast
          az deployment group create --resource-group DeployARM --template-file template.json
